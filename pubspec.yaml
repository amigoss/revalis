name: snie_agent
description: SNIE Agent de terrain — Revalis Digital. Saisie de traçabilité offline-first.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  geolocator: ^12.0.0
  shared_preferences: ^2.2.3
  uuid: ^4.4.0
  intl: ^0.19.0
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true
